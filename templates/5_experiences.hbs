<section id="experiences" class="dark-bg light-typo">

  <div class="container">
    <header class="section-header text-center">

      <h1 class="scrollimation scale-in">Experiences</h1>

    </header>

      {{#each experiences}}
      <div class="row margin-bottom20">
        <div class="col-sm-12 col-md-4 text-col img-center scrollimation fade-right" style="text-align: center;">

          <img src='{{ this.img.src }}' width='{{ this.img.width }}' height='{{ this.img.height }}' />

          <div class="sum">
            <em>{{ this.description }}</em>
          </div>

          <div class="tech">
             {{#each this.tech}}
              <span class="label label-white">{{this}}</span>
            {{/each}}
          </div>

        </div>

        <div class="col-sm-12 col-md-8 scrollimation fade-left text-center-sm margin-top10-sm">

          <div class="profession">
            <h2 class="font-size1x1-sm">{{ this.position }}</h2>
          </div>
          <div class="date">{{ this.date }}{{#if this.duration}} ({{this.duration}}){{/if}} - {{ this.location }}</div>
          <div class="description">
            <ul>
              {{#each this.descriptionLi}}
                <li>{{this}}</li>
              {{/each}}
            </ul>
          </div>
          <div class='website text-center-sm'>
            <a class="btn btn-meflat no-margin white icon-right" href="{{this.link}}" target='_blank'>Visit Website<i class="fa fa-rocket"></i></a>
          </div>

        </div>
            </div>
      {{/each}}


  </div>

</section>
